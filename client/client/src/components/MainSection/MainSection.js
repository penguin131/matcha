import React from 'react'
import css from './MainSection.module.css'

const MainSection = () => {
    return (
        <section className={css.mainSectionContainer}>MainSection</section>
    )
}

export default MainSection
