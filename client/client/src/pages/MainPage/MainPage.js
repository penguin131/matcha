import React from 'react'
import css from './MainPage.module.less'

const MainPage = () => {
  return (
    <section className={css.mainSectionContainer}>MainPage</section>
  )
}

export default MainPage
