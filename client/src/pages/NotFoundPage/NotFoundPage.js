import React from 'react'
import css from './NotFoundPage.module.less'

const NotFoundPage = () => {
	return (
		<div className={css.mainSectionContainer}>Такой страницы нет</div>
	)
}

export default NotFoundPage
